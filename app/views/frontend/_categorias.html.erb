<header>
  <h3 class="cabecera">Categorías</h3>
</header>
<div class="row">
  <% @categorias.each_with_index do |categoria, index| %>
      <div class="col-xs-6 categorias">
        <%= image_tag categoria.image.url(:resume), class: 'img-responsive' %>
      </div>
      <% if index.odd? %>
      <div class="col-xs-12"></div>
      <% end %>
  <% end %>
</div>